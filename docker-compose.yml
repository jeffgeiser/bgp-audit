version: '3.8'

services:
  bgp-audit:
    build: .
    container_name: bgp_audit_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_DIR=/app/data
    restart: unless-stopped
